# New-cat 小程序 PRD v1

版本：v1.0  
范围：微信小程序 + 共用后端 + 共用数据模型  
目标：先把“宠物效期管理”做成可上线、可审核、可持续扩展的 MVP

## 1. 产品定位
New-cat 小程序是网页端 `pawday.cn / New-cat` 的移动端主入口，核心是宠物、产品、疫苗、用药、计划、记录和提醒管理。

v1 不做电商，不做订单，不做复杂家庭共享，不做完整图片上传闭环，但必须预留后续能力，不把未来能力写死。

## 2. 已确认的产品规则
1. 小程序和网页端共用一套后端与核心数据。
2. 小程序以微信登录为主，网页端保留邮箱登录。
3. 会员只有两档：免费会员、付费会员。其他档位预留，不在 v1 展开。
4. 会员升级/续费先走“人工开通 / 扫码联系”。
5. v1 不做订单记录。
6. v1 不做电商。
7. v1 不做家庭共享，但数据结构预留。
8. v1 不做完整图片上传闭环，但必须预留上传入口和字段。
9. 提醒方式以小程序订阅消息和站内通知为主，公众号提醒、更多提醒方式后续预留。
10. 网页端与小程序的会员状态、绑定状态、提醒状态需要同步。

## 3. 数据契约原则
这里的“冻结”不是冻结产品功能，而是冻结核心数据的最小契约。

冻结的内容：
- 实体名称
- 主键和关联方式
- 状态枚举的基础含义
- 列表页、详情页、编辑页的核心字段
- 登录、绑定、会员、提醒的状态语义

允许后续扩展：
- 新增字段
- 新增页面
- 新增枚举值
- 新增子功能
- 新增提醒方式

禁止一开始就写死：
- “不支持上传”
- “永久不开放”
- “只有这一种状态”
- “以后不能扩展”

## 4. v1 页面范围
页面清单以 `PAGE_STRUCTURE_V1.md` 为准，当前保持 32 页。

一级导航：
- 首页
- 宠物
- 产品
- 通知
- 我的

二级模块：
- 疫苗
- 用药
- 计划
- 记录
- 认证
- 协议
- 设置
- 反馈
- 会员

### 4.1 一级页与二级页对应关系
| 一级页 | 二级页 / 子入口 | 说明 |
|---|---|---|
| 首页 | 宠物详情、产品详情、疫苗详情、用药详情、计划详情、通知详情、会员中心、人工开通页 | 首页承接总览、提醒、快捷入口和跳转 |
| 宠物 | 宠物新增、宠物详情、疫苗列表、用药列表、计划列表、记录列表 | 宠物是核心对象入口，其他健康模块可从宠物视角进入 |
| 产品 | 产品新增、产品详情、产品分类管理 | 产品页承接效期、开封、关联宠物等管理 |
| 通知 | 通知详情 | 统一承接站内通知、到期提醒、系统提醒 |
| 我的 | 个人资料编辑、会员中心、人工开通页、设置、反馈、用户协议、隐私政策、修改密码、登录注册、账号绑定 | 我的页承接账号、会员、配置和合规入口 |

### 4.2 二级模块挂载关系
| 二级模块 | 主要入口来源 | 说明 |
|---|---|---|
| 疫苗 | 首页、宠物详情、我的 | 作为宠物健康管理的二级功能，不单独占一级导航 |
| 用药 | 首页、宠物详情、我的 | 作为宠物健康管理的二级功能，不单独占一级导航 |
| 计划 | 首页、宠物详情、我的 | 计划与提醒绑定，保留扩展空间 |
| 记录 | 首页、宠物详情、我的 | 记录模块支持历史查看与后续图片扩展 |
| 认证 | 登录、注册、忘记密码、重置密码、绑定页 | 账号体系的认证环节，作为流程页存在 |
| 协议 | 我的、登录前引导 | 用户协议和隐私政策必须可达 |
| 设置 | 我的 | 作为个人配置入口存在 |
| 反馈 | 我的 | 作为意见反馈入口存在 |
| 会员 | 首页、我的 | 会员中心和人工开通页保留升级入口 |

### 4.3 首页页面级详细内容
| 状态 / 模块 | 核心内容 | 主要操作 | 说明 |
|---|---|---|---|
| 未登录态 | 欢迎区、产品名、产品说明、登录入口、注册入口 | 进入登录、进入注册 | 不展示用户业务数据 |
| 已登录问候区 | 用户昵称、今日问候、会员状态 | 进入会员中心 | 会员状态要与网页端同步 |
| 统计卡片 | 宠物数量、正常库存数、临期产品数、过期产品数 | 查看概览、跳转对应模块 | 首页总览核心区域 |
| 疫苗提醒 | 即将到期疫苗、宠物名称、疫苗名称、下次接种日期 | 查看全部、进入疫苗列表/详情 | 最多展示近期重点内容 |
| 用药提醒 | 即将用药记录、宠物名称、药品名称、下次用药日期 | 查看全部、进入用药列表/详情 | 与提醒中心数据一致 |
| 快捷操作 | 添加宠物、添加库存、记录疫苗、记录用药、健康计划、健康记录 | 跳转对应新增页或列表页 | 首页作为高频操作入口 |

#### 4.3.1 首页入口规则
- 首页不新增一级导航，只作为总览和快捷入口。
- 未登录态只展示欢迎和登录/注册，不展示业务数据。
- 已登录态必须展示会员状态、统计摘要和近期提醒。
- 快捷操作必须覆盖宠物、产品、疫苗、用药、计划、记录。
- 首页展示内容可以随数据为空而隐藏，但入口不能缺失。

### 4.4 我的页面级详细内容
| 模块 | 核心内容 | 主要操作 | 说明 |
|---|---|---|---|
| 未登录态 | 默认头像、未登录提示、登录/注册入口 | 进入登录/注册 | 我的页未登录时不展示个人数据 |
| 个人卡片 | 头像、昵称、邮箱、会员状态、编辑入口 | 进入个人资料编辑 | 头像字段预留，v1 不做完整上传闭环 |
| 快捷统计 | 我的宠物、产品库存、通知中心 | 切换到对应 Tab | 我的页保留常用业务入口 |
| 会员区 | 会员中心、人工开通 | 进入会员中心、进入人工开通页 | 承接升级/续费路径 |
| 管理区 | 疫苗记录、用药记录、健康计划、健康记录 | 进入对应二级模块 | 作为健康模块的集中入口 |
| 设置区 | 个人资料、提醒设置、修改密码 | 编辑资料、保存设置、修改密码 | 承接账号配置 |
| 其他与合规区 | 意见反馈、用户协议、隐私政策 | 提交反馈、查看协议、查看隐私政策 | 满足审核与用户服务要求 |
| 退出登录 | 退出当前账号 | 确认退出 | 仅登录态展示 |

#### 4.4.1 我的页入口规则
- 我的页承接账号、会员、设置、协议、隐私、反馈和退出登录。
- 会员中心和人工开通页必须在我的页可达。
- 疫苗、用药、计划、记录可以从我的页作为二级入口进入。
- 个人资料编辑保留头像字段，但当前阶段不做完整上传闭环。
- 合规入口必须长期保留，不能只放在登录页。

## 5. 核心功能范围
### 5.1 账户与认证
- 微信登录
- 邮箱登录
- 邮箱注册
- 忘记密码
- 重置密码
- 微信与网页账号绑定

### 5.2 宠物
- 新增
- 编辑
- 列表
- 详情
- 预留头像字段

#### 5.2.1 宠物二级页面详细内容
| 页面 | 核心内容 | 主要操作 | 说明 |
|---|---|---|---|
| 宠物列表 | 名称、种类、品种、性别、生日、年龄、体重、状态 | 搜索、筛选、进入详情、新增 | 宠物模块主入口 |
| 宠物新增 / 编辑 | 名称、种类、品种、性别、生日、体重、绝育、过敏、慢病、长期用药、特殊护理、备注 | 保存、取消、返回 | v1 预留头像字段，但不做完整上传闭环 |
| 宠物详情 | 基础信息、健康摘要、关联产品、关联疫苗、关联用药、关联计划、关联记录 | 编辑、删除、进入关联模块 | 详情页是其他健康模块的分发入口 |

#### 5.2.2 宠物页内部规则
- 宠物是所有健康数据的归属主体。
- 宠物新增必须允许先保存最基础字段，再逐步补充扩展字段。
- 宠物头像字段要预留，但当前阶段不做完整上传闭环。
- 宠物详情页要能跳到疫苗、用药、计划和记录。

### 5.3 产品
- 新增
- 编辑
- 列表
- 详情
- 分类管理
- 保质期与开封提醒

#### 5.3.1 产品二级页面详细内容
| 页面 | 核心内容 | 主要操作 | 说明 |
|---|---|---|---|
| 产品列表 | 名称、分类、批次、效期、开封状态、剩余天数、关联宠物、提醒状态 | 搜索、筛选、进入详情、新增 | 产品一级入口，承接效期管理 |
| 产品新增 / 编辑 | 名称、分类、自定义分类、批次、规格、生产日期、过期日期、开封日期、数量、提醒开关、关联宠物、备注 | 保存、取消、选择分类、选择宠物 | 当前代码已有核心表单骨架 |
| 产品详情 | 基础信息、效期状态、开封状态、提醒时间线、关联宠物、备注、操作按钮 | 编辑、标记开封、删除 | 详情页承接后续扩展字段 |
| 产品分类管理 | 预制分类、用户自定义分类、排序、描述、启用状态 | 新增分类、编辑分类、启用/停用 | 先保留入口，后续可独立成管理页或抽屉 |

#### 5.3.2 产品页内部规则
- 产品是效期管理的核心对象，不做电商购买流程。
- 产品必须支持与宠物关联，但不要求强制绑定。
- 产品必须支持“开封后重新计算剩余天数”的逻辑预留。
- 产品分类必须同时支持预制分类和用户自定义分类。
- 产品提醒必须区分未到期、临近到期、已过期、已用完。
- 分类管理先保证数据结构可扩展，页面形态后续再定。
- 后续如果补图片，优先用于分类或产品视觉，不影响现有字段。

### 5.4 疫苗
- 新增
- 编辑
- 列表
- 详情
- 下次提醒

#### 5.4.1 疫苗二级页面详细内容
| 页面 | 核心内容 | 主要操作 | 说明 |
|---|---|---|---|
| 疫苗列表 | 疫苗名称、宠物、接种日期、下次提醒、医院、医生、提醒状态 | 搜索、筛选、进入详情、新增 | 可从宠物详情进入，也可从我的页进入 |
| 疫苗新增 / 编辑 | 宠物、疫苗名称、品牌、剂量、接种日期、下次接种日期、提醒开关、提前天数、医院、医生、备注 | 保存、取消 | 适配单宠物管理和后续批量提醒 |
| 疫苗详情 | 基础信息、提醒状态、历史记录、备注、关联宠物 | 编辑、删除 | v1 以记录和提醒为主 |

#### 5.4.2 疫苗页内部规则
- 疫苗必须关联宠物。
- 疫苗提醒以日期提醒为主。
- 是否提醒和提前几天必须可配置。
- 未来如增加批次、图片、接种凭证，可直接扩字段，不改主流程。

### 5.5 用药
- 新增
- 编辑
- 列表
- 详情
- 周期提醒

#### 5.5.1 用药二级页面详细内容
| 页面 | 核心内容 | 主要操作 | 说明 |
|---|---|---|---|
| 用药列表 | 药名、宠物、用途、剂量、频率、下一次用药、提醒状态 | 搜索、筛选、进入详情、新增 | 用药模块主入口 |
| 用药新增 / 编辑 | 宠物、药名、类型、剂量、频率、频率天数、开始时间、结束时间、下次用药、提醒开关、提前天数、备注 | 保存、取消 | 适配驱虫、治疗、长期维护等场景 |
| 用药详情 | 基础信息、执行记录、提醒状态、备注、关联宠物 | 编辑、删除 | 后续可接执行打卡或服药确认 |

#### 5.5.2 用药页内部规则
- 用药必须关联宠物。
- 频率要支持固定频率和自定义频率。
- 提醒要支持按周期和按日期两种思路预留。
- 后续如增加执行打卡，不改现有字段主干。

### 5.6 计划
- 新增
- 编辑
- 列表
- 详情
- 重复规则

#### 5.6.1 计划二级页面详细内容
| 页面 | 核心内容 | 主要操作 | 说明 |
|---|---|---|---|
| 计划列表 | 计划名、宠物、关联类型、周期、状态、下次执行时间 | 搜索、筛选、进入详情、新增 | 用于统一管理提醒和循环任务 |
| 计划新增 / 编辑 | 宠物、计划名、目标类型、开始时间、结束时间、重复规则、提醒方式、提醒开关、备注 | 保存、取消 | 适配疫苗、体检、驱虫、护理等计划 |
| 计划详情 | 日历、执行状态、历史、下次执行时间、备注 | 编辑、删除 | 计划是一个可循环任务对象 |

#### 5.6.2 计划页内部规则
- 计划必须关联宠物。
- 周期支持日、周、月、季、年和自定义。
- 计划详情页要保留日历视图入口。
- 后续支持完成标记时，不改计划主结构。

### 5.7 记录
- 健康记录列表
- 健康记录详情
- 预留图片字段

#### 5.7.1 记录二级页面详细内容
| 页面 | 核心内容 | 主要操作 | 说明 |
|---|---|---|---|
| 记录列表 | 时间、类型、宠物、摘要、筛选条件 | 搜索、筛选、进入详情、新增 | 统一承接健康事件历史 |
| 记录新增 / 编辑 | 宠物、记录类型、记录日期、数值/结果、备注、预留图片字段 | 保存、取消 | 当前以文本和数值为主，图片仅预留 |
| 记录详情 | 时间、类型、对象、结果、备注、关联提醒 | 编辑、删除 | 用于查看历史健康事件 |

#### 5.7.2 记录页内部规则
- 记录必须关联宠物。
- 记录类型要支持扩展。
- 图片字段预留，但 v1 不做完整上传闭环。
- 未来如果做健康档案图片，不影响当前记录字段。

### 5.8 通知
- 系统通知
- 到期提醒
- 已读未读状态

#### 5.8.1 通知二级页面详细内容
| 页面 | 核心内容 | 主要操作 | 说明 |
|---|---|---|---|
| 通知列表 | 标题、来源、时间、已读/未读、跳转目标 | 查看、标记已读、全部已读 | 统一承接所有提醒消息 |
| 通知详情 | 标题、正文、来源对象、创建时间、关联跳转 | 返回、跳转来源页 | 作为消息落点 |

#### 5.8.2 通知页内部规则
- 通知必须保留已读未读状态。
- 通知要能跳回对应业务页。
- 到期提醒、系统通知、会员通知都放在同一通知中心。

### 5.9 会员
- 会员中心
- 人工开通说明 / 二维码
- 权益说明
- 续费入口保留

#### 5.9.1 会员二级页面详细内容
| 页面 | 核心内容 | 主要操作 | 说明 |
|---|---|---|---|
| 会员中心 | 免费/付费状态、到期时间、权益列表、续费入口 | 查看权益、进入人工开通页 | 个人中心必须可达 |
| 人工开通说明 / 二维码 | 说明文字、二维码、联系说明、备注 | 保存二维码、联系人工开通 | v1 用人工方式补齐付费闭环 |

#### 5.9.2 会员页内部规则
- 会员只保留免费和付费两档。
- 续费入口先走人工开通。
- 未来接支付时不改会员中心主结构。

## 6. 图片上传策略
v1 不做完整图片上传，但要预留：
- 用户头像
- 宠物头像
- 健康记录图片

预留方式：
- 字段预留
- 页面入口预留
- 服务层方法预留
- 后端接口命名预留

## 7. 上线前提
- AppID 确认
- 微信开放平台绑定确认
- 后端地址确认
- 合法域名确认
- 隐私政策、用户协议、权限说明齐全
- 订阅消息模板确认

## 8. 开发顺序建议
1. 先定数据契约和页面骨架
2. 再做登录、绑定、会员壳
3. 再做宠物、产品、疫苗、用药的核心 CRUD
4. 再做计划、记录、通知
5. 最后做会员开通、预留上传、发布校验

## 9. 最终确认前页面明细总表
本章节用于开发前最终确认。确认后，v1 开发不再调整一级/二级页面主结构，只允许补充非破坏性字段。

| 页面 | 状态/模块 | 字段与内容 | 主要交互 | 后端依赖 | 预留项 |
|---|---|---|---|---|---|
| 首页 | 未登录态 | 欢迎区、产品名、产品说明、登录入口、注册入口 | 登录、注册 | 否 | 新手引导 |
| 首页 | 已登录态 | 昵称、会员状态、宠物数、库存数、临期数、过期数、疫苗提醒、用药提醒、快捷操作 | 进入会员、进入新增页、进入列表页 | 是 | 更多提醒卡片 |
| 登录 | 邮箱登录 | 邮箱、密码、记住邮箱、协议勾选、错误提示 | 登录、忘记密码、注册、查看协议/隐私 | 是 | 微信登录入口 |
| 注册 | 邮箱注册 | 邮箱、验证码、密码、确认密码、协议勾选 | 获取验证码、注册、返回登录、查看协议/隐私 | 是 | 手机号注册 |
| 忘记密码 | 找回密码 | 邮箱、发送状态、返回登录 | 发送重置邮件、返回登录 | 是 | 图形验证码 |
| 重置密码 | 重置密码 | token、新密码、确认新密码、无效链接提示 | 确认重置、返回登录 | 是 | 短信验证码 |
| 宠物列表 | 列表 | 名称、种类、品种、性别、生日、年龄、体重、状态、搜索/筛选 | 新增、进入详情 | 是 | 头像展示 |
| 宠物新增/编辑 | 表单 | 名称、种类、品种、性别、生日、体重、绝育、过敏、慢病、长期用药、特殊护理、备注 | 保存、取消 | 是 | 宠物头像上传 |
| 宠物详情 | 详情 | 基础信息、健康摘要、关联产品、疫苗、用药、计划、记录 | 编辑、删除、进入关联模块 | 是 | 家庭共享 |
| 产品列表 | 列表 | 名称、分类、批次、效期、开封状态、剩余天数、关联宠物、提醒状态 | 搜索、筛选、新增、进入详情 | 是 | 分类管理入口 |
| 产品新增/编辑 | 表单 | 名称、分类、自定义分类、批次、规格、生产日期、过期日期、开封日期、数量、提醒开关、关联宠物、备注 | 保存、取消、选择分类、选择宠物 | 是 | 产品/分类图片 |
| 产品详情 | 详情 | 基础信息、效期状态、开封状态、提醒时间线、关联宠物、备注、操作按钮 | 编辑、标记开封、删除 | 是 | 使用记录 |
| 产品分类管理 | 能力预留 | 分类名称、描述、排序、预制/自定义来源、启用状态 | 新增、编辑、启用/停用 | 是 | 独立路由 |
| 疫苗列表 | 列表 | 疫苗名称、宠物、接种日期、下次提醒、医院、医生、提醒状态 | 搜索、筛选、新增、进入详情 | 是 | 接种凭证 |
| 疫苗新增/编辑 | 表单 | 宠物、疫苗名称、品牌、剂量、接种日期、下次接种日期、提醒开关、提前天数、医院、医生、备注 | 保存、取消 | 是 | 批次/凭证图片 |
| 疫苗详情 | 详情 | 基础信息、提醒状态、历史记录、备注、关联宠物 | 编辑、删除 | 是 | 历史时间线 |
| 用药列表 | 列表 | 药名、宠物、用途、剂量、频率、下一次用药、周期、提醒状态 | 搜索、筛选、新增、进入详情 | 是 | 执行状态 |
| 用药新增/编辑 | 表单 | 宠物、药名、类型、剂量、频率、频率天数、开始时间、结束时间、下次用药、提醒开关、提前天数、备注 | 保存、取消 | 是 | 服药打卡 |
| 用药详情 | 详情 | 基础信息、执行记录、提醒状态、备注、关联宠物 | 编辑、删除 | 是 | 执行记录闭环 |
| 计划列表 | 列表 | 计划名、宠物、关联类型、周期、状态、下次执行时间 | 搜索、筛选、新增、进入详情 | 是 | 日历视图 |
| 计划新增/编辑 | 表单 | 宠物、计划名、目标类型、开始时间、结束时间、重复规则、提醒方式、提醒开关、备注 | 保存、取消 | 是 | 多提醒方式 |
| 计划详情 | 详情 | 日历、执行状态、历史、下次执行时间、备注 | 编辑、删除 | 是 | 完成打卡 |
| 记录列表 | 列表 | 时间、类型、宠物、摘要、筛选条件 | 搜索、筛选、新增、进入详情 | 是 | 图片缩略图 |
| 记录新增/编辑 | 表单 | 宠物、记录类型、记录日期、数值/结果、备注、预留图片字段 | 保存、取消、删除 | 是 | 健康记录图片 |
| 通知中心 | 列表 | 标题、来源、时间、已读/未读、跳转目标 | 查看、标记已读、全部已读 | 是 | 订阅消息跳转 |
| 我的 | 未登录态 | 默认头像、未登录提示、登录/注册入口 | 登录/注册 | 否 | 微信授权头像 |
| 我的 | 已登录态 | 头像、昵称、邮箱、会员状态、绑定状态、快捷入口、退出登录 | 编辑资料、进入会员、进入设置、退出登录 | 是 | 账号合并 |
| 个人资料编辑 | 表单 | 昵称、头像预留、邮箱、手机号、微信绑定状态、基础资料 | 保存、返回 | 是 | 头像上传 |
| 设置 | 配置 | 提醒开关、提前提醒天数、功能开关、数据说明 | 保存、开关切换 | 是 | 更多提醒方式 |
| 修改密码 | 表单 | 当前密码、新密码、确认新密码 | 保存、返回 | 是 | 强度校验 |
| 意见反馈 | 表单 | 反馈类型、内容、联系方式、提交状态 | 提交、返回 | 是 | 图片反馈 |
| 用户协议 | 合规 | 标题、版本号、生效日期、正文 | 阅读 | 否 | 版本记录 |
| 隐私政策 | 合规 | 标题、版本号、生效日期、正文 | 阅读 | 否 | 隐私指引 |
| 会员中心 | 权益 | 免费/付费状态、到期时间、权益列表、升级入口 | 查看权益、人工开通 | 是 | 多档位会员 |
| 人工开通 | 说明 | 说明文字、二维码、联系说明、备注 | 联系人工开通 | 否 | 在线支付 |
