# PRD v1 架构约束修订

## 1. 现阶段不做不等于永久不做
所有在 v1 暂不实现的能力，都必须按“后续可扩展”处理，不能在代码、数据结构、接口或文案里写死为永久不支持。

## 2. 图片上传预留
v1 不实现完整图片上传闭环，但必须预留用户上传入口和字段。

预留范围：
- 用户头像
- 宠物头像
- 健康记录图片
- 后续图片存储接口
- 后续图片回显和删除
- 后续审核与合规提示

v1 实现口径：
- 页面可保留入口或占位状态
- 数据类型保留图片字段
- API 层可预留上传方法命名
- 不接真实上传、存储、审核和回显闭环
- 不写“不支持上传”这类永久否定文案

## 3. 其他后续能力预留
以下能力 v1 暂不实现，但架构必须预留：
- 家庭共享
- 公众号提醒
- 多会员档位
- 在线支付
- 订单记录
- 更多通知方式
- 多端账号绑定和合并

## 4. 产品文案规则
对未开放能力，统一使用：
- “暂未开放”
- “后续开放”
- “当前阶段暂不提供”

禁止使用：
- “不支持”
- “不会做”
- “无法上传”
- “永久关闭”

## 5. 开发规则
后续开发必须遵守：
- 数据模型不写死单一形态
- 枚举保留扩展空间
- 页面结构保留入口或兼容字段
- API 服务层避免把未实现能力删除干净
- v1 不暴露未完成闭环，避免影响微信审核
